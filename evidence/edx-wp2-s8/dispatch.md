# WP2-S8 dispatch

strict/core-bounded，新mutation API與atomic contract已固定；native Worker Jason/high/clean fork_context=false/shared sequential product writer。runtime繼承model（未另指定）；主線期間只control/evidence。
Native gate首輪STRICT_REVIEW_REQUIRED，主線把預留Independent Review明示為1（沿Owner既有外部review交回流程，不即時另spawn Reviewer）；更新後PASS，prompt1384bytes，1 Worker／1 planned Reviewer／0 Repair。只派一名implementation agent，candidate階段不當成Independent GO。
主線CodeGraph semantic query找到deck-spec identity與renderer source；source核對manual geometry可render非slot components、registry尚無insert-element。S7 closure74d63cc已merge/push main及S7 branch，遠端readback一致。
