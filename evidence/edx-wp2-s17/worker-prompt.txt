實作 EDX-WP2-S17。工作樹 /Users/matt/Documents/ChatGPT/skill 工廠/PPTSKILL-canonical，branch codex/edx-wp2-s17-text-double-click。先完整讀 tasks/edx-wp2-s17-text-double-click.md，契約以此為準。此卡已完成 CodeGraph prequery、S16 source/protected/ZIP baseline 及 scope 裁決。
你是唯一 product writer，直接編輯指定工作樹；Mainline 同時只準備驗收。沿既有 S16 selectedTextTarget/openEditText，增加安全 component dblclick 入口，維持 S1 role editing，不新增 contenteditable/writer/schema。write scope 僅卡內四個 runtime/test/harness 檔案；必要 mounted double 薄改須說明。不要改 control、BACKLOG、evidence、ZIP、protected4 或 AI Core；不 commit/push/build/full/browser/啟 Chrome。
先寫真行為 RED，再最小 GREEN；RED/GREEN/scoped raw logs 存 /private/tmp/pptskill-s17-worker-red.log、/private/tmp/pptskill-s17-worker-green.log、/private/tmp/pptskill-s17-worker-scoped.log，保留原 FAIL。新增 --text-double-click-regression 與 bounded browser cases，使用 S16 harness 既有 helper。scoped 檔名依 task 先核對。最多一次實作 generation；同類兩次無進展停止重判。不要另開 agent。
完成回報：改動檔案、RED/GREEN/scoped 計數與 log paths、runtime/harness 邊界及未解問題。不要跑 browser 或宣稱 browser 已驗；由 Mainline fresh 驗收。
