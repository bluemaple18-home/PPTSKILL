# VQ1-GRAMMAR-001 Receipt

- 工作名稱：Golden Design Grammar portable contract
- 狀態：`PASS / P0-VQ1 REMAINS PROVENANCE PARTIAL`
- traces_to：`US-001`、`US-003`、`FR-001`、`FR-003`、`SC-001`、`P0-VQ1`
- frontier：`VQ1-GRAMMAR-001`
- blocking_edges：Golden Cover reference intake 與 Owner corpus acceptance 已完成
- 不包含：P0-VQ2 renderer、封面重畫、外部 runtime、第三方素材複製

## 交付

- 建立自足的 `golden-design-grammar.v1.json`。
- 建立 deterministic `compileVisualRouteCandidate()`。
- 建立依 route + semantic role 解析的 `resolveRoleTreatments()`。
- 十個 seed archetypes 均具有可重組結構契約與 accepted evidence refs。
- 保留 `none` / reduced-motion / validated resting geometry。
- 未更動既有 DeckSpec sanitizer 與 renderer。

## Deterministic acceptance

- Grammar schema/version、catalog references、effect family bound 與 semantic roles 可驗證。
- 十個 archetype 均可編譯，且結構 signature 不退化成單一 shared grid。
- 未知 token fail loud。
- Route contract 不含完整 HTML、CSS 或外部 URL dependency。
- 既有全套測試須維持通過。

## 剩餘風險

- 15 張 accepted reference 仍只有穩定本機 evidence key，沒有可證明的歷史 reference id。
- 本 slice 不做 browser visual acceptance；沒有產生或修改 browser-visible renderer。
- P0-VQ2 必須以本契約取代舊有 shared `8fr / 4fr` cover skeleton，並另過 Owner 視覺閘門。
