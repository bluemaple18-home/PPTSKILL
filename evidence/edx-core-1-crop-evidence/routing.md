# 本卡內 mapping routing

標準、唯讀、clean native Worker1、Reviewer0、並行repo writer0；preflight PASS。native可用模型未提供Luna/Terra，依tool契約繼承parent Astra，Worker reasoning medium，未調主對話模型。runtime agent James / 01a0cf88-ef51-7342-9db5-edd6ab673a4e；不是新visible task，未fork_context。
本輪先補architecture measured gap，不安裝vendor、不改delivery。主線負責S18收尾／Crop裁決，Worker限卡內mapping；standard研究不默認新增Reviewer。後續若production schema風險達strict，再在同一卡依實際风险套既有gate。

Implementation同卡升strict：schema/portable/lifecycle跨面，沿同一James Worker接續（非第二Worker），planned Reviewer1於candidate穩定後才建立；repair0。preflight PASS，shared writer1僅Worker，Mainline停止repo寫入。模型沿原native Astra medium，Mainline不改主模型；後續Review採獨立clean context，按實際風險。

## 實際交付 routing

全卡只有同一James Worker與同一clean Curie Reviewer；Worker完成Repair1後停筆並關閉，沒有Repair2、第二產品writer、新visible task或額外主卡。Mainline執行固定candidate完整非browser／ZIP／managed host驗收與control/evidence整理。Reviewer fresh focused27/scoped339及故障注入，最後只讀已提交browser/PGQ/host05補證，沒有另啟browser。歷史初次5P2與剩餘F2 P2完整保留，按常設規範納入Final Closure residual。模型沿runtime實際lane，未自行降低Owner主對話模型。
